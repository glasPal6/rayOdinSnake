# odinSnake

Odin + raylib + ANGLE test project.

## Build

```bash
make
```

## Run

```bash
make run
```

## Layout

- `src/` — source code
- `raylib/` — Odin raylib bindings
- `angle/` — ANGLE GLES/Metal dylibs (copied next to the binary at build time)
